# tmux モード Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** VK Terminals(Electron/GUI) の代わりに tmux を実行面にできる「tmux モード」を vk-orchestrator に追加する。

**Architecture:** 実行面クライアント `src/terminals/index.js` を「共通ロジック層（index.js）＋ バックエンド層（backend-vk-terminals.js / backend-tmux.js）」に分割する。index.js が設定 `terminals.mode` でバックエンドを選び、8 個のプリミティブ（health/getStates/createNewPane/send/title/prUrl/waiting/menu）をそこへ委譲する。高位ロジック（submitToClaude 等）はプリミティブの上に組まれているためバックエンド非依存で共有される。

**Tech Stack:** Node.js 20+ (ESM), `node --test`, tmux CLI（`new-window` / `send-keys` / `capture-pane` / `list-windows` / `has-session`）, child_process。

## Global Constraints

- Node.js **20 以上**（`.node-version` = 20.18.2）。
- 既定モードは `vk-terminals`。**VK Terminals モードのコード経路・設定・挙動は不変**（全既存テスト `node --test tests/*.test.js` がそのままパスすること）。
- ブロックマークアップ等は無関係。純粋な Node ロジック。
- 承認プロンプト検知など「端末を読んで進捗判断する」機能は**追加しない**（現状踏襲）。
- 設定解決の優先順位は既存踏襲：**env > config.json > 既定**。
- 実装ブランチ: `feature/tmux-mode`（作成済み）。設計: `docs/superpowers/specs/2026-07-14-tmux-mode-design.md`。

---

## File Structure

- Create: `src/terminals/backend-vk-terminals.js` — 現行 HTTP プリミティブ 8 個を移設（挙動不変）。
- Create: `src/terminals/backend-tmux.js` — `createTmuxBackend(opts)` ファクトリ。同じ 8 プリミティブを tmux コマンドへ写像。
- Modify: `src/terminals/index.js` — バックエンド選択＋委譲。高位ロジック（submitToClaude / waitForClaudeReady / findIdleTerminal / getTerminalBaseline / confirmOutputProgressed / waitForHealth / setOwnPaneTitle / buildPaneTitleSequence）は残す。
- Modify: `src/config.js` — `TERMINALS_MODES` / `resolveTerminalsMode` / `resolveTmuxSession` / `resolveTmuxClaudeCommand` を追加。
- Modify: `bin/vk-orchestrator.js` — `up` を mode で分岐（tmux では Electron を起動せず tmux セッションを用意してから engine を起動）。
- Create tests: `tests/terminalsMode.test.js`, `tests/backendTmux.test.js`。

---

## Task 1: config に mode / tmux 設定の解決関数を追加

**Files:**
- Modify: `src/config.js`（`getVkTerminalsGpuMode` 近傍、GPU_MODES と同じ書式）
- Test: `tests/terminalsMode.test.js`（新規）

**Interfaces:**
- Produces:
  - `export const TERMINALS_MODES = ['vk-terminals', 'tmux']`
  - `resolveTerminalsMode(cfg?) => 'vk-terminals' | 'tmux'`（env `VK_TERMINALS_MODE` > `cfg.terminals.mode` > 既定 `'vk-terminals'`。未知値は既定へフォールバック）
  - `resolveTmuxSession(cfg?) => string`（env `VK_TMUX_SESSION` > `cfg.tmux.session` > `'vk-orch'`）
  - `resolveTmuxClaudeCommand(cfg?) => string`（env `VK_TMUX_CLAUDE_CMD` > `cfg.tmux.claudeCommand` > `'claude'`）

- [ ] **Step 1: 失敗するテストを書く** — `tests/terminalsMode.test.js`

```javascript
import { test, beforeEach, afterEach } from 'node:test';
import assert from 'node:assert/strict';
import {
  TERMINALS_MODES,
  resolveTerminalsMode,
  resolveTmuxSession,
  resolveTmuxClaudeCommand,
} from '../src/config.js';

const ENV_KEYS = ['VK_TERMINALS_MODE', 'VK_TMUX_SESSION', 'VK_TMUX_CLAUDE_CMD'];
let saved;
beforeEach(() => { saved = {}; for (const k of ENV_KEYS) { saved[k] = process.env[k]; delete process.env[k]; } });
afterEach(() => { for (const k of ENV_KEYS) { if (saved[k] === undefined) delete process.env[k]; else process.env[k] = saved[k]; } });

test('resolveTerminalsMode: 既定は vk-terminals', () => {
  assert.equal(resolveTerminalsMode({}), 'vk-terminals');
});
test('resolveTerminalsMode: config で tmux', () => {
  assert.equal(resolveTerminalsMode({ terminals: { mode: 'tmux' } }), 'tmux');
});
test('resolveTerminalsMode: env が config より優先', () => {
  process.env.VK_TERMINALS_MODE = 'tmux';
  assert.equal(resolveTerminalsMode({ terminals: { mode: 'vk-terminals' } }), 'tmux');
});
test('resolveTerminalsMode: 未知値は既定へフォールバック', () => {
  assert.equal(resolveTerminalsMode({ terminals: { mode: 'bogus' } }), 'vk-terminals');
});
test('TERMINALS_MODES は vk-terminals と tmux', () => {
  assert.deepEqual(TERMINALS_MODES, ['vk-terminals', 'tmux']);
});
test('resolveTmuxSession: 既定 vk-orch / env 優先', () => {
  assert.equal(resolveTmuxSession({}), 'vk-orch');
  process.env.VK_TMUX_SESSION = 'foo';
  assert.equal(resolveTmuxSession({ tmux: { session: 'bar' } }), 'foo');
});
test('resolveTmuxClaudeCommand: 既定 claude / config 反映', () => {
  assert.equal(resolveTmuxClaudeCommand({}), 'claude');
  assert.equal(resolveTmuxClaudeCommand({ tmux: { claudeCommand: 'claude --dangerously-skip-permissions' } }),
    'claude --dangerously-skip-permissions');
});
```

- [ ] **Step 2: 失敗を確認**

Run: `node --test tests/terminalsMode.test.js`
Expected: FAIL（`resolveTerminalsMode` などが export されていない）

- [ ] **Step 3: 最小実装** — `src/config.js` の `getVkTerminalsGpuMode` の直後あたりに追加

```javascript
/** 実行面モードの取りうる値。 */
export const TERMINALS_MODES = ['vk-terminals', 'tmux'];

let warnedUnknownTerminalsMode = false;

/**
 * 実行面モードを解決する。優先順位: env VK_TERMINALS_MODE > config(terminals.mode) > 既定。
 * 未知値は既定 'vk-terminals' へフォールバックし、一度だけ警告する（起動は止めない）。
 * @param {object} [cfg] loadUnifiedConfig() の戻り値
 * @returns {'vk-terminals'|'tmux'}
 */
export function resolveTerminalsMode(cfg = loadUnifiedConfig()) {
  const raw = String(process.env.VK_TERMINALS_MODE ?? cfg?.terminals?.mode ?? '').trim().toLowerCase();
  if (TERMINALS_MODES.includes(raw)) return raw;
  if (raw !== '' && !warnedUnknownTerminalsMode) {
    warnedUnknownTerminalsMode = true;
    console.warn(`[Config] 未知の実行面モード "${raw}" は無視し、既定 "vk-terminals" を使用します（有効値: ${TERMINALS_MODES.join(' / ')}）。`);
  }
  return 'vk-terminals';
}

/**
 * tmux モードで使うセッション名。env VK_TMUX_SESSION > config(tmux.session) > 'vk-orch'。
 * @param {object} [cfg]
 * @returns {string}
 */
export function resolveTmuxSession(cfg = loadUnifiedConfig()) {
  const raw = String(process.env.VK_TMUX_SESSION ?? cfg?.tmux?.session ?? '').trim();
  return raw || 'vk-orch';
}

/**
 * tmux モードで新規ペイン作成時に起動する Claude コマンド。
 * env VK_TMUX_CLAUDE_CMD > config(tmux.claudeCommand) > 'claude'。
 * bypass 運用は 'claude --dangerously-skip-permissions' 等をここで指定する（コードに埋めない）。
 * @param {object} [cfg]
 * @returns {string}
 */
export function resolveTmuxClaudeCommand(cfg = loadUnifiedConfig()) {
  const raw = String(process.env.VK_TMUX_CLAUDE_CMD ?? cfg?.tmux?.claudeCommand ?? '').trim();
  return raw || 'claude';
}
```

- [ ] **Step 4: テストがパスすることを確認**

Run: `node --test tests/terminalsMode.test.js`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add src/config.js tests/terminalsMode.test.js
git commit -m "[ 機能追加 ] config に実行面モード(terminals.mode)と tmux 設定の解決関数を追加"
```

---

## Task 2: 現行プリミティブを backend-vk-terminals.js に抽出（挙動不変リファクタ）

**Files:**
- Create: `src/terminals/backend-vk-terminals.js`
- Modify: `src/terminals/index.js:1-288`（プリミティブ定義を移設し、委譲へ差し替え）
- Test: 既存 `tests/createNewPane.test.js` / `tests/submitToClaude.test.js` / `tests/setTerminalPrUrl.test.js` / `tests/postMenu.test.js` / `tests/setExternalWaiting.test.js`（**変更しない**。全パス維持が受け入れ条件）

**Interfaces:**
- Produces（`backend-vk-terminals.js` の named export、シグネチャは現行 index.js と完全一致）:
  - `checkHealth(port, opts?)`, `getStates(port)`, `createNewPane(port, cwd?, options?)`,
    `sendToTerminal(port, termId, input)`, `setTerminalTitle(port, termId, title, url?)`,
    `setTerminalPrUrl(port, termId, prUrl, opts?)`, `setExternalWaiting(port, termId, waiting)`,
    `postMenu(port, section, opts?)`
- Consumes: なし（この 8 個は自己完結。`apiHost` / `BASE_URL` ヘルパもここへ移す）

- [ ] **Step 1: backend-vk-terminals.js を作成**

現行 `src/terminals/index.js` から以下を**そのまま**移設する：モジュール冒頭の `apiHost` / `BASE_URL`（現 9-10 行）と、8 プリミティブ関数本体（`checkHealth` 現56、`getStates` 現100、`createNewPane` 現122、`sendToTerminal` 現138、`setTerminalTitle` 現153、`setTerminalPrUrl` 現192、`setExternalWaiting` 現240、`postMenu` 現271）。各関数の実装は 1 バイトも変えない。ファイル冒頭コメントに「VK Terminals HTTP バックエンド」と明記。

```javascript
// VK Terminals(HTTP API) バックエンド。実行面として Electron/GUI の VK Terminals を
// 127.0.0.1:13847 の HTTP API 経由で駆動する。ここにある 8 プリミティブは
// src/terminals/index.js から移設したもので、挙動は不変。
const apiHost = () => process.env.VK_TERMINALS_HOST ?? '127.0.0.1';
const BASE_URL = (port) => `http://${apiHost()}:${port}`;

export async function checkHealth(port, { timeoutMs = 3_000 } = {}) { /* 現 index.js 56-66 と同一 */ }
export async function getStates(port) { /* 現 100-103 と同一 */ }
export async function createNewPane(port, cwd = null, options = {}) { /* 現 122-135 と同一 */ }
export async function sendToTerminal(port, termId, input) { /* 現 138-145 と同一 */ }
export async function setTerminalTitle(port, termId, title, url = null) { /* 現 153-166 と同一 */ }
export async function setTerminalPrUrl(port, termId, prUrl, { prMerged = false } = {}) { /* 現 192-226 と同一 */ }
export async function setExternalWaiting(port, termId, waiting) { /* 現 240-256 と同一 */ }
export async function postMenu(port, section, { timeoutMs = 3_000 } = {}) { /* 現 271-288 と同一 */ }
```

注: `setTerminalPrUrl` は内部で `getStates(port)` を呼ぶ。この呼び出しは**同ファイル内のローカル `getStates`**（VK 実装）を指すよう、同ファイルの関数を参照させる（挙動不変のため）。

- [ ] **Step 2: index.js をバックエンド委譲へ差し替え**

`src/terminals/index.js` から 8 プリミティブと `apiHost` / `BASE_URL` の定義を削除。代わりに backend-vk-terminals を import し、同名の委譲関数を定義する（この Task では常に VK バックエンドへ委譲。Task 4 で mode 分岐を入れる）。`CLEAR_INPUT_SEQUENCE` と高位ロジック（`buildPaneTitleSequence` / `setOwnPaneTitle` / `waitForHealth` / `findIdleTerminal` / `getTerminalBaseline` / `confirmOutputProgressed` / `pickEchoFragment` / `confirmBodyEchoed` / `waitForClaudeReady` / `submitToClaude`）はそのまま残す。

```javascript
import { stripControlChars } from '../engine/build-command.js';
import * as vkBackend from './backend-vk-terminals.js';

// Task 4 で resolveTerminalsMode による分岐に差し替える。現段階は常に VK バックエンド。
function backend() { return vkBackend; }

export const checkHealth        = (...a) => backend().checkHealth(...a);
export const getStates          = (...a) => backend().getStates(...a);
export const createNewPane      = (...a) => backend().createNewPane(...a);
export const sendToTerminal     = (...a) => backend().sendToTerminal(...a);
export const setTerminalTitle   = (...a) => backend().setTerminalTitle(...a);
export const setTerminalPrUrl   = (...a) => backend().setTerminalPrUrl(...a);
export const setExternalWaiting = (...a) => backend().setExternalWaiting(...a);
export const postMenu           = (...a) => backend().postMenu(...a);

const CLEAR_INPUT_SEQUENCE = '\x01\x0b';
// …以下、buildPaneTitleSequence / setOwnPaneTitle / waitForHealth / findIdleTerminal /
//   getTerminalBaseline / confirmOutputProgressed / pickEchoFragment / confirmBodyEchoed /
//   waitForClaudeReady / submitToClaude は現行のまま残す。
//   これら内部の getStates(...) / sendToTerminal(...) 呼び出しは上の委譲 export を指す。
```

注意: 高位ロジック内の `getStates` / `sendToTerminal` 参照が、上で定義した委譲 `export const` を指していることを確認する（同一モジュール内の同名束縛なので自然にそうなる）。

- [ ] **Step 3: 既存テスト全体を実行して非回帰を確認**

Run: `node --test tests/*.test.js`
Expected: PASS（createNewPane / submitToClaude / setTerminalPrUrl / postMenu / setExternalWaiting すべて緑。global.fetch モックが委譲経由で VK バックエンドに届く）

- [ ] **Step 4: コミット**

```bash
git add src/terminals/index.js src/terminals/backend-vk-terminals.js
git commit -m "[ リファクタ ] 実行面クライアントをバックエンド層(VK Terminals HTTP)へ分離"
```

---

## Task 3: tmux バックエンドを実装

**Files:**
- Create: `src/terminals/backend-tmux.js`
- Test: `tests/backendTmux.test.js`（新規）

**Interfaces:**
- Produces: `createTmuxBackend({ session, claudeCommand, run }) => backend`
  - `run(args: string[]) => { status: number, stdout: string, stderr: string }`（省略時は tmux CLI を spawnSync で実行する既定ランナー）
  - backend は Task 2 と同じ 8 プリミティブ名を持ち、`port` は無視する。
  - `getStates(port)` の戻り: `{ terminals: { [winId]: { termId, waiting, lastOutputTime, lastLines } } }`（`lastOutputTime` は **ミリ秒**。`#{window_activity}` 秒 ×1000）
  - createNewPane が返す termId は tmux の `window_id`（例 `@3`）。
  - backend は生成済みペインを内部 Map で追跡し、`getStates` は**そのペインのみ**報告する（セッション初期ウィンドウやユーザーが開いた窓は無視）。

- [ ] **Step 1: 失敗するテストを書く** — `tests/backendTmux.test.js`

```javascript
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { createTmuxBackend } from '../src/terminals/backend-tmux.js';

// tmux 実行を差し替えるフェイク。呼ばれた args を記録し、指定した stdout を返す。
function fakeRunner(responses = {}) {
  const calls = [];
  const run = (args) => {
    calls.push(args);
    const key = args.join(' ');
    for (const [prefix, out] of Object.entries(responses)) {
      if (key.startsWith(prefix)) return { status: 0, stdout: out, stderr: '' };
    }
    return { status: 0, stdout: '', stderr: '' };
  };
  return { run, calls };
}

test('createNewPane: new-window で window_id を作り termId に返す', async () => {
  const { run, calls } = fakeRunner({ 'new-window': '@3\n' });
  const be = createTmuxBackend({ session: 'vk-orch', claudeCommand: 'claude', run });
  const termId = await be.createNewPane(0, '/work/dir', {});
  assert.equal(termId, '@3');
  const nw = calls.find(a => a.includes('new-window'));
  assert.ok(nw.includes('-t') && nw.includes('vk-orch'));
  assert.ok(nw.includes('-c') && nw.includes('/work/dir'));
  assert.ok(nw.join(' ').includes('claude')); // claudeCommand を起動
});

test('createNewPane: noClaude なら claude を起動しない', async () => {
  const { run, calls } = fakeRunner({ 'new-window': '@4\n' });
  const be = createTmuxBackend({ session: 'vk-orch', claudeCommand: 'claude', run });
  await be.createNewPane(0, null, { noClaude: true });
  const nw = calls.find(a => a.includes('new-window'));
  assert.ok(!nw.join(' ').includes('claude'));
});

test('sendToTerminal: \\r は Enter、本文はリテラル送信', async () => {
  const { run, calls } = fakeRunner();
  const be = createTmuxBackend({ session: 'vk-orch', claudeCommand: 'claude', run });
  await be.sendToTerminal(0, '@3', 'hello world');
  await be.sendToTerminal(0, '@3', '\r');
  const literal = calls.find(a => a.includes('-l'));
  assert.deepEqual(literal, ['send-keys', '-t', '@3', '-l', '--', 'hello world']);
  const enter = calls.find(a => a.includes('Enter'));
  assert.deepEqual(enter, ['send-keys', '-t', '@3', 'Enter']);
});

test('getStates: 生成済みペインのみ、lastOutputTime は ms', async () => {
  const { run } = fakeRunner({
    'new-window': '@3\n',
    'list-windows': '@3 1000\n@9 1000\n',   // @9 は追跡外 → 報告しない
    'capture-pane': 'line1\nline2\n',
  });
  const be = createTmuxBackend({ session: 'vk-orch', claudeCommand: 'claude', run });
  await be.createNewPane(0, null, {});
  const { terminals } = await be.getStates(0);
  const ids = Object.keys(terminals);
  assert.deepEqual(ids, ['@3']);
  assert.equal(terminals['@3'].termId, '@3');
  assert.equal(terminals['@3'].lastOutputTime, 1000 * 1000);
  assert.match(terminals['@3'].lastLines, /line2/);
  assert.equal(terminals['@3'].waiting, false);
});

test('setExternalWaiting: getStates に反映される往復ストア', async () => {
  const { run } = fakeRunner({ 'new-window': '@3\n', 'list-windows': '@3 5\n', 'capture-pane': 'x\n' });
  const be = createTmuxBackend({ session: 'vk-orch', claudeCommand: 'claude', run });
  await be.createNewPane(0, null, {});
  await be.setExternalWaiting(0, '@3', true);
  const { terminals } = await be.getStates(0);
  assert.equal(terminals['@3'].waiting, true);
});

test('checkHealth: has-session の status 0 で true', async () => {
  const run = (args) => args.includes('has-session') ? { status: 0, stdout: '', stderr: '' } : { status: 1, stdout: '', stderr: '' };
  const be = createTmuxBackend({ session: 'vk-orch', claudeCommand: 'claude', run });
  assert.equal(await be.checkHealth(0), true);
});

test('setTerminalPrUrl / postMenu は no-op で {ok:true}', async () => {
  const be = createTmuxBackend({ session: 'vk-orch', claudeCommand: 'claude', run: () => ({ status: 0, stdout: '', stderr: '' }) });
  assert.deepEqual(await be.setTerminalPrUrl(0, '@3', 'http://x'), { ok: true });
  assert.deepEqual(await be.postMenu(0, {}), { ok: true });
});
```

- [ ] **Step 2: 失敗を確認**

Run: `node --test tests/backendTmux.test.js`
Expected: FAIL（`createTmuxBackend` 未定義）

- [ ] **Step 3: 最小実装** — `src/terminals/backend-tmux.js`

```javascript
// tmux バックエンド。1 tmux window = 1 セッション。port は無視する。
// VK Terminals の getStates 形（{terminals:{id:{termId,waiting,lastOutputTime,lastLines}}}）に合わせる。
import { spawnSync } from 'node:child_process';

const CAPTURE_LINES = 40; // capture-pane で取る末尾行数（lastLines のエコー確認に十分な長さ）

function defaultRun(args) {
  const r = spawnSync('tmux', args, { encoding: 'utf8' });
  return { status: r.status ?? 1, stdout: r.stdout ?? '', stderr: r.stderr ?? '' };
}

/**
 * @param {object} opts
 * @param {string} opts.session       対象 tmux セッション名
 * @param {string} opts.claudeCommand 新規ペインで起動する Claude コマンド
 * @param {(args:string[])=>{status:number,stdout:string,stderr:string}} [opts.run] tmux ランナー（テスト差し替え用）
 */
export function createTmuxBackend({ session, claudeCommand, run = defaultRun }) {
  // 生成したペインのみ追跡する（初期ウィンドウ・ユーザーが開いた窓は対象外）。
  const panes = new Map(); // winId -> { waiting: boolean }

  async function checkHealth() {
    return run(['has-session', '-t', session]).status === 0;
  }

  async function createNewPane(_port, cwd = null, options = {}) {
    const args = ['new-window', '-t', session, '-P', '-F', '#{window_id}'];
    if (cwd) args.push('-c', cwd);
    if (!options.noClaude) args.push('--', 'sh', '-c', claudeCommand);
    const r = run(args);
    const winId = r.stdout.trim();
    if (r.status !== 0 || !winId) throw new Error(`tmux new-window failed: ${r.stderr || 'no window id'}`);
    panes.set(winId, { waiting: false });
    return winId;
  }

  async function sendToTerminal(_port, termId, input) {
    if (input === '\r' || input === '\n') {
      run(['send-keys', '-t', termId, 'Enter']);
    } else {
      run(['send-keys', '-t', termId, '-l', '--', input]);
    }
    return { ok: true };
  }

  async function getStates() {
    const r = run(['list-windows', '-t', session, '-F', '#{window_id} #{window_activity}']);
    const activity = new Map();
    for (const line of r.stdout.split('\n')) {
      const [id, sec] = line.trim().split(/\s+/);
      if (id) activity.set(id, Number(sec) || 0);
    }
    const terminals = {};
    for (const [winId, state] of panes) {
      if (!activity.has(winId)) continue; // 窓が消えた → 報告しない（pane-missing 検知に乗る）
      const cap = run(['capture-pane', '-p', '-t', winId, '-S', `-${CAPTURE_LINES}`]);
      terminals[winId] = {
        termId: winId,
        waiting: state.waiting,
        lastOutputTime: activity.get(winId) * 1000, // 秒→ms（VK Terminals は Date.now() ms）
        lastLines: cap.stdout,
      };
    }
    return { terminals };
  }

  async function setExternalWaiting(_port, termId, waiting) {
    const p = panes.get(String(termId)) ?? panes.get(termId);
    if (p) p.waiting = !!waiting;
    return { ok: true };
  }

  async function setTerminalTitle(_port, termId, title) {
    if (title) run(['rename-window', '-t', termId, String(title)]);
    return { ok: true };
  }

  // 飾り系。tmux では表示対象が無いので no-op。
  async function setTerminalPrUrl() { return { ok: true }; }
  async function postMenu() { return { ok: true }; }

  return {
    checkHealth, getStates, createNewPane, sendToTerminal,
    setTerminalTitle, setTerminalPrUrl, setExternalWaiting, postMenu,
  };
}
```

- [ ] **Step 4: テストがパスすることを確認**

Run: `node --test tests/backendTmux.test.js`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add src/terminals/backend-tmux.js tests/backendTmux.test.js
git commit -m "[ 機能追加 ] tmux バックエンド(createTmuxBackend)を実装"
```

---

## Task 4: index.js のバックエンド選択を mode で切替

**Files:**
- Modify: `src/terminals/index.js`（Task 2 で置いた `backend()` を mode 分岐に）
- Test: `tests/terminalsMode.test.js`（Task 1 のファイルにルーティング検証を追記）

**Interfaces:**
- Consumes: `resolveTerminalsMode`（Task 1）, `createTmuxBackend`（Task 3）, `resolveTmuxSession` / `resolveTmuxClaudeCommand`（Task 1）
- Produces: index.js の委譲プリミティブが、mode に応じて VK / tmux のどちらかへ届く。バックエンドは初回解決時に一度だけ生成しメモ化（実行中に mode は変わらない前提。ポーリングごとの config 再読込を避ける）。

- [ ] **Step 1: 失敗するテストを追記** — `tests/terminalsMode.test.js` の末尾に追加

```javascript
test('index.js: tmux モードでは tmux バックエンドへルーティングされる', async () => {
  process.env.VK_TERMINALS_MODE = 'tmux';
  // tmux CLI を叩かせないよう、send-keys だけ検証できる軽い確認にする。
  // グローバル fetch をトラップし、呼ばれたら失敗させる（＝HTTP バックエンドに落ちていない証明）。
  const originalFetch = global.fetch;
  global.fetch = async () => { throw new Error('fetch must not be called in tmux mode'); };
  try {
    // モジュールキャッシュをまたぐため、動的 import で新しい評価にする
    const mod = await import(`../src/terminals/index.js?tmux=${Date.now()}`);
    // has-session が無い環境でも throw しないこと（checkHealth は false を返すだけ）
    const ok = await mod.checkHealth(0);
    assert.equal(typeof ok, 'boolean');
  } finally {
    global.fetch = originalFetch;
    delete process.env.VK_TERMINALS_MODE;
  }
});
```

- [ ] **Step 2: 失敗を確認**

Run: `node --test tests/terminalsMode.test.js`
Expected: FAIL（現状 `backend()` が常に vkBackend を返し、`checkHealth` が fetch を呼ぶため throw）

- [ ] **Step 3: 実装** — `src/terminals/index.js` の `backend()` を差し替え

```javascript
import * as vkBackend from './backend-vk-terminals.js';
import { createTmuxBackend } from './backend-tmux.js';
import { resolveTerminalsMode, resolveTmuxSession, resolveTmuxClaudeCommand } from '../config.js';

let _backend = null;
function backend() {
  if (_backend) return _backend;
  if (resolveTerminalsMode() === 'tmux') {
    _backend = createTmuxBackend({
      session: resolveTmuxSession(),
      claudeCommand: resolveTmuxClaudeCommand(),
    });
  } else {
    _backend = vkBackend;
  }
  return _backend;
}
```

- [ ] **Step 4: テスト確認（新規＋全体非回帰）**

Run: `node --test tests/terminalsMode.test.js && node --test tests/*.test.js`
Expected: PASS（tmux ルーティング成立、かつ既定 vk-terminals の全既存テストも緑）

- [ ] **Step 5: コミット**

```bash
git add src/terminals/index.js tests/terminalsMode.test.js
git commit -m "[ 機能追加 ] 実行面バックエンドを terminals.mode で選択（vk-terminals / tmux）"
```

---

## Task 5: bin `up` を tmux モードで分岐（Electron を起動せず tmux で回す）

**Files:**
- Modify: `bin/vk-orchestrator.js:308`（`case 'up'` の冒頭で mode 分岐）
- Test: 手動確認（bin の副作用が大きくユニット化しづらいため、手順で検証）

**Interfaces:**
- Consumes: `resolveTerminalsMode` / `resolveTmuxSession`（config）, `checkHealth`（terminals, tmux モードでは has-session）
- Produces: tmux モードの `up` は「セッション確保 → engine 起動」。VK Terminals モードは現行の Electron 起動フローのまま（不変）。

- [ ] **Step 1: `case 'up'` の先頭に tmux 分岐を追加**

`bin/vk-orchestrator.js` の `case 'up': {` 直後（現 320 行 `const { spawn } = ...` の**前**）に挿入する。

```javascript
    case 'up': {
      // --- tmux モード分岐 ---
      // 実行面が tmux のときは Electron(VK Terminals) を一切起動しない。
      // 対象 tmux セッションを用意し、orchestrator ループ（= start と同じ）を
      // このプロセスのフォアグラウンドで回す。ワーカーペインは engine が
      // tmux new-window で作り、利用者は `tmux attach -t <session>` で覗く。
      {
        const { resolveTerminalsMode, resolveTmuxSession } = await import('../src/config.js');
        if (resolveTerminalsMode(unifiedConfig) === 'tmux') {
          const { spawnSync } = await import('child_process');
          const session = resolveTmuxSession(unifiedConfig);
          const has = spawnSync('tmux', ['has-session', '-t', session], { stdio: 'ignore' });
          if (has.status !== 0) {
            const created = spawnSync('tmux', ['new-session', '-d', '-s', session], { stdio: 'inherit' });
            if (created.status !== 0) {
              console.error(`[up] tmux セッション "${session}" を作成できませんでした（tmux は入っていますか？）。`);
              process.exit(1);
            }
            console.log(`tmux セッション "${session}" を作成しました。`);
          } else {
            console.log(`tmux セッション "${session}" に接続します。`);
          }
          console.log(`各ワーカーは \`tmux attach -t ${session}\` で覗けます。orchestrator を起動します...`);
          await reconcileOrchestratorVersion();
          await import('../src/engine/index.js'); // start と同じくループを起動
          break;
        }
      }
      // --- 以降は従来の VK Terminals(Electron) 起動フロー（不変） ---
      const { spawn } = await import('child_process');
      // …現行のまま…
```

- [ ] **Step 2: VK Terminals モードの非回帰を確認**

Run: `node --test tests/*.test.js`
Expected: PASS（bin 変更は分岐追加のみ。既定 vk-terminals 経路は不変）

- [ ] **Step 3: tmux モードの手動スモークテスト**

前提: ローカルに tmux がある。実際の GitHub 実行は不要で「Electron を起動せず tmux セッションが立ち、engine が動き出す」ことだけ確認する。

Run:
```bash
tmux kill-session -t vk-orch 2>/dev/null; \
VK_TERMINALS_MODE=tmux VK_TMUX_SESSION=vk-orch node bin/vk-orchestrator.js up
```
Expected: Electron ウィンドウが**開かない**。`tmux セッション "vk-orch" を作成しました。` が出て、orchestrator のループログが流れ始める。別端末で `tmux has-session -t vk-orch` が status 0。
確認後 Ctrl-C で停止し、`tmux kill-session -t vk-orch` で掃除。

- [ ] **Step 4: コミット**

```bash
git add bin/vk-orchestrator.js
git commit -m "[ 機能追加 ] up を tmux モードで分岐（Electron を起動せず tmux セッションで起動）"
```

---

## Self-Review（記入済み）

- **Spec coverage:**
  - 「共通ロジック層＋バックエンド層」→ Task 2 / Task 4。
  - 「tmux 写像（new-window / send-keys / capture-pane / window_activity / 飾り no-op）」→ Task 3。
  - 「Electron を起動しない起動フロー」→ Task 5。
  - 「設定 terminals.mode / tmux.session / claudeCommand」→ Task 1。
  - 「挙動互換（既存テスト全パス）」→ Task 2 Step 3・Task 4 Step 4・Task 5 Step 2 で明示。
  - 「承認検知は作らない」→ どのタスクにも承認検知の実装は無い（設計どおり）。
- **Placeholder scan:** コード引用の `/* 現 index.js NN-MM と同一 */` は「既存コードの逐語移設」を指す具体指示であり、対象行を明記済み。曖昧な TODO は無し。
- **Type consistency:** 8 プリミティブ名は 3 ファイルで一致（checkHealth/getStates/createNewPane/sendToTerminal/setTerminalTitle/setTerminalPrUrl/setExternalWaiting/postMenu）。`getStates` の戻り形（terminals→{termId,waiting,lastOutputTime(ms),lastLines}）は VK 実装と tmux 実装で一致。termId は tmux では window_id 文字列で、engine 側は `String(t.termId)` 比較のため整合。
